============
Installation
============

At the command line::

    $ easy_install cookiecutter-pytesting

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cookiecutter-pytesting
    $ pip install cookiecutter-pytesting
